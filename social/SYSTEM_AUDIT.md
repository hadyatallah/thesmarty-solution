# TSS system audit — 16 September 2026

## Verified working

Five user-approved Feed posts published to both Instagram and Facebook; actual
exports inspected. Post 5 completion is recorded on the state branch at
`ae893b80c93df47f5b737e5edcaae9121b3f2ade`.
The account checks and original 34 regressions passed in workflow `35088599365`.

The 44-asset plan, exact approval binding, source freshness, dimensions, logo,
OCR, actual media checks, geographic evidence and duplicate history are present.
Website/enquiry/Sheets/Microsoft 365/SEO/Metricool settings were outside the audit.
No credential values were read or changed.

## Corrections made

- Recurring Cyprus-time checks with an effective readiness gate distinct from
  the user's authorization to automate. All ten exact approvals and inspected
  live formats remain required; scheduled events cannot use controlled exceptions.
- One attempt per run even if a later platform or verification fails. Known live
  IDs remain visible, uncertainty halts further attempts, and every actual asset
  is archived. Caption checking preserves meaningful signs and punctuation.
- Actual-asset hash binding for every format's live verification.
- PNG/JPEG export extension follows the actual bytes.
- New draft planning respects monthly topic/format allocations, reviewed input
  eligibility, recent content and the current design rather than the old card
  fallback. Exhausted inputs are reported, not silently replaced with inventions.
- Performance learning is scoped to comparable format/objective. New factual
  content requires primary source metadata. New Reel duration matches its script.
- Stale documentation replaced with actual capability and launch requirements.

- Publisher policy/version preflight prevents an outdated deployment from
  reserving a newly approved post and then falsely quarantining it.

Validation: all **61 regression tests passed** locally, including real image
exports, OCR, video decoding and preservation of first-ten approval bindings.
The deployment workflow repeats these checks against the connected accounts.

## Remaining launch work

1. Prepare current-design final revisions of Posts 6–10, recheck sources and
   complete final exported-file QA; obtain Hady's individual approval for each.
2. Use the approved initial Stories/Reel for controlled publication. Inspect the
   actual results and record their hash-bound format proofs. Story and Reel
   remain ineligible for unattended publication until that succeeds.
3. Supply a rolling, source-reviewed content bank and finished queue covering
   the 44-asset plan. Existing input is six evergreen briefs and zero reviewed
   trends. Research, factual interpretation, final rendering and visual review
   are not autonomously performed by the GitHub publisher.
4. Facebook currently receives Feed images. Facebook Stories/Reels need separate
   adapters and controlled tests if the full format mix is wanted there too.

The scheduled workflow is authorized and begins with readiness checks. It can
publish due approved queue items automatically once the launch gates are all
satisfied. It neither approves Posts 6–10 nor manufactures content to fill slots.
The live state is `tss-social-state: social/automation-status.json`.
