name: TSS Instagram and Facebook Automation
run-name: TSS social readiness, QA and publishing

on:
  push:
    branches: [main]
    paths:
      - 'content-queue/**'
      - 'social/**'
      - 'api/publish-instagram.js'
      - 'api/social-creative.js'
      - '.github/workflows/publish-instagram.yml'
      - 'package-lock.json'
  workflow_dispatch:
  schedule:
    # UTC wake-ups cover Cyprus summer (+03) and winter (+02) time.
    # The queue's offset-aware publishAt prevents early publication. Extra
    # wake-ups retry a delayed/missed trigger, never a recorded publication.
    - cron: '15,45 6,7,11,12,16,17 * * *'

# Explicit automatic-after-QA authorization replaces the first-ten rollout hold.
# Actual content checks, supported adapters, duplicate protection and caps remain.
permissions:
  contents: write

concurrency:
  group: tss-instagram-publisher
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout the current main branch
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
          persist-credentials: false
      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund
      - name: Install final-export verification tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq tesseract-ocr tesseract-ocr-eng ffmpeg
      - name: Test fail-closed publishing gates
        run: node --test social/tests/*.test.js
      - name: Inspect accounts, history and the final publishing queue
        env:
          TSS_PUBLISHER_KEY: ${{ secrets.TSS_PUBLISHER_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        run: node social/scripts/process-queue.js
      - name: Report monthly content supply without approving drafts
        if: ${{ !cancelled() && hashFiles('social-results/history.json') != '' }}
        run: node social/scripts/check-content-plan.js
      - name: Check inspected QA samples without publishing
        env:
          TSS_PUBLISHER_KEY: ${{ secrets.TSS_PUBLISHER_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        run: node social/scripts/check-samples.js
      - name: Verify unpublished final review exports without publishing
        if: ${{ !cancelled() }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        run: node social/scripts/check-review-batches.js
      - name: Retain QA and actual published asset evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tss-social-qa-${{ github.run_id }}
          path: social-results/
          if-no-files-found: ignore
          retention-days: 30
